{
  "name": "aws-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf function.zip",
    "build": "yarn clean && yarn install --production=true && zip -r function.zip ."
  },
  "dependencies": {
    "firebase-admin": "^11.6.0"
  }
}
